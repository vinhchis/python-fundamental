# LIST

- list of numbers
- list of strings
- list of mix
- empty list

list.append(item) -> add a item
list.remove(item) -> remove a item or item[index]

Check if Existed in List
```python
if 1 in [1, 2, 3, 4, 5]:
    print("True")
```